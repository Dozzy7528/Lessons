#include <iostream>
using namespace std;

int main()
{
	cout << "Enter four numbers, each on a new line:\n";
	float m, v, g, h;
	float end;
	end = ((m * (v * v) / 2) + (m * g * h));
	cout << "Output of program:" << " " << end;
}